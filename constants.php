<?php

define( 'WP2MD_ROOT', dirname( __FILE__ ) );

if ( !defined( 'WP2MD_CHARSET' ) ) {
	define( 'WP2MD_CHARSET', 'UTF-8' );
}

if ( !defined( 'WP2MD_ROOT_URL' ) ) {
	define( 'WP2MD_ROOT_URL', './' );
}

if ( !defined( 'WP2MD_AUTORUN' ) ) {
	define( 'WP2MD_AUTORUN', false );
}
